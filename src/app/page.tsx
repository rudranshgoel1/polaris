import { Button } from "@/components/ui/button"

const Page = () => {
    return (
        <div>
            <Button variant={"default"}>
                Click me
            </Button>
        </div>
    );
};

export default Page;